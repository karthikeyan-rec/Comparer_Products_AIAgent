# Secure Gemini API Key — Task List

- [x] Create `package.json` with express + dotenv dependencies
- [x] Create `server.js` — Express proxy server
- [x] Create `.env.example` — template for users
- [x] Create `.env` — actual key file (user fills in)
- [x] Create `.gitignore` — exclude .env and node_modules
- [ ] Modify `agent.js` — point Gemini fetch to `/api/gemini` proxy
- [ ] Modify `index.html` — hide API key input for Gemini mode, show lock badge
- [ ] Run `npm install` to install dependencies
- [ ] Verify server starts correctly
